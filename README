This is the backend repository which will run all the logics i.e time and date validation and insert/update/delete/get the data from database. 
Please run the frontend repository as well for the system to function well.

run npm i to install all dependencies

paste below in .env file:
PORT=5000
ATLAS_URI="mongodb+srv://NadhirahM:harihDanNitaf@cluster0.pxlv8.mongodb.net/Booking?retryWrites=true&w=majority"

to start server: npm run start
